console.log(`"--anouncejs connected--"`);

// const small = document.querySelector(".big");
// const big = document.querySelector(".small");

// let deg = 0;

// setInterval(() => {
//     deg = deg+ 45;
//     // console.log(deg);
//     small.style.transform = `rotate(${deg}deg)`;
//     big.style.transform = `rotate(-${deg}deg)`;
// }, 1500);
